<nav class ="navbar navbar-expand-sm navbar-dark bg-primary mb-3">
    <div class="container">
       <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mobile-nav">
            <span class="navbar-toggle-icon"></span>
       </button>
    </div>
    <div class="collapse navbar-collapse" id="mobile-nav">
        <ul class="navbar-nav m1-auto">
            {%if app.user %}
                <li class="nav-item">
                    <a href="{{ path('top_up') }}" class="nav-link">Top-Up</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('home_url') }}" class="nav-link">Users</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_logout') }}" class="nav-link">Logout</a>
                </li>
            {% else %}
                <li class="nav-item">
                    <a href="{{ path('app_register') }}" class="nav-link">Register</a>
                </li>
                <li class="nav-item">
                    <a href="{{ path('app_login') }}" class="nav-link">login</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>